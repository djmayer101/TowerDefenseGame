/*package com.game.mainTest;

import static org.junit.Assert.assertEquals;

import java.util.concurrent.CopyOnWriteArrayList;

import org.junit.Test;
import org.junit.Before;

import android.graphics.Point;

import com.game.main.PathBuilder;
import com.game.main.TerrainMap;
import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.stub;
import static org.mockito.Mockito.verify;

public class PathBuilderTest{


	private TerrainMap myTerrainMap;
	
	private PathBuilder myPathBuilder;

	@Before
	public void setup(){
		
		//myTowerManager = new TowerManager();
		//myPathBuilder = new PathBuilder(myTerrainMap,myTowerManager);
	}
	
	@Test
	public void startPostionEqualsEndPosition(){
		//CopyOnWriteArrayList<Point> path = myPathBuilder.run(new Point(0,0), new Point(0,0));
		assertEquals("work",1,1);
	}
}
*/